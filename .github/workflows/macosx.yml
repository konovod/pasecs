name: MacOSX CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Install FPC
        run: |
          brew update
          brew install lazarus
      - uses: actions/checkout@v2
      - name: Run tests
        run: lazbuild bench/benchmark.lpr
